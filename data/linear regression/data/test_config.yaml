"""
That's config can be used for change model params
"""

path_to_data: "data/train.csv"
target: "SalePrice"
path_to_weights: "data/linear.yaml"
path_to_result: "data/result.csv"
mode: "test"
model:
  bias: 0.00
  alpha: 0.01
  max_epochs: 200
